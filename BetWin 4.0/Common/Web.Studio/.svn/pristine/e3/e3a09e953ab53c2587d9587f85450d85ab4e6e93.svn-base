(function ($, window) {
    var setter = $.setter;

    $.utils = {
        // 是否是主页
        "isHomePage": function (views) {
            var isHomePage = false;
            setter.homePage.forEach(setter.homePage, function (item) {
                
            });
        }
    };


})(mui, window);